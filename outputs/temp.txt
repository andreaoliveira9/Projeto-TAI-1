



#include "CArray.h"

int CArrayTests()
{
    printf("\n");
    }
}
struct Stack *createStack()
{
    struct Stack *stack = malloc(sizeof(struct ListNode));
        tmp->data = x;
        tmp->next = ptr->next;
            ptr->next->next = NULL;
            }
        }

        res += d * ord;          ord *= 10;
    }

    printf("\n the number in binary is: ");

        for (j = i - gap; j >= 0 && array[j] > array[j + gap]; j = j - gap)
                array[i] = rand() % range + 1;

    show_data(array, size
