



#include "CArray.h"

int CArrayTests()
{
    printf("\n");
    for (i = 0; i <= counter; i++)
            {
            i++;
        } while (arr[j] > pivot);

        
        if (digit_count == MAX_DIGITS)
        {
                result = 1;
                printAVL(root, 1);

            break;
    case 0x1B:          exit(EXIT_SUCCESS);
    }
}
#endif


int main(int argc, const char *argv[])
{
    double X0 = 0.f;          
    double X_MAX = 10.F;      
    double Y0[] = {1.f, 0.f}; 
    double step_size
